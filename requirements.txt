# AEON - Core dependencies for Phase 1 (solid foundation)

# Web framework
fastapi>=0.112.0
uvicorn[standard]>=0.30.0

# AI / Local inference
ollama>=0.2.0

# Data validation & settings
pydantic>=2.0.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# Utilities
loguru>=0.7.0

# Frontend is managed separately with npm
# Testing & dev tools (optional but recommended)
pytest>=7.4.0
black>=23.9.0
